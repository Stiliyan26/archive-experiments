package bg.latona.santa.repositories;

import bg.latona.santa.entities.HashTag;
import bg.latona.santa.entities.QHashTag;

public interface HashTagRepository extends CommonRepository<HashTag, QHashTag, Long> {
	
}