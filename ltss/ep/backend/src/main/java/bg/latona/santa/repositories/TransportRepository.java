package bg.latona.santa.repositories;

import bg.latona.santa.entities.transport.QTransport;
import bg.latona.santa.entities.transport.Transport;

public interface TransportRepository extends CommonRepository<Transport, QTransport, Long> {
	
}